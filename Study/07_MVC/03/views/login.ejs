<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>result</title>
  <style>
    div {
      width: 200px;
      height: 200px;
      border: 2px solid black;
    }
  </style>
</head>
<body>
  <h1>result</h1>
  <div class="result"></div>
  <div class="userInfo"></div>

  <script>
    console.log('success');
    console.log(id, pw);
    const result = document.querySelector('.result');
    const userInfo = document.querySelector('.userInfo');

    for(let i = 0; i < user.length; i++) { 
      console.log(i);
      if(user[i].id == id && user[i].pw == pw) { 
        result.textContent = 'Login success';
        userInfo.textContent = `User name : user[i].userName \n User age : user[i].userAge`;
        loginSuccess = true;
        break;
      } 
      else {
        result.textContent = 'Login fail please try again';
      }
    } 
  </script>
</body>
</html>